name: Greetings

on: 
  issues:
    types: [opened, edited]

jobs:
  greeting:
    runs-on: ubuntu-latest
    permissions:
      issues: write
    steps:
    - name: get issue detail
      run: |
        echo '${{ toJSON(job) }}'
        echo '${{ toJSON(github) }}'
        echo '${{ github.event.issue.body }}'
        cat ${{ github.event_path }}
